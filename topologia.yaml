name: ebpf-mqtt-lab

topology:
  nodes:
    # VÍTIMA: O Gateway que roda o Mosquitto + eBPF/XDP
    victim:
      kind: linux
      image: ubuntu:22.04
      binds:
        - ./ebpf-code:/src
      exec:
        - apt-get update && apt-get install -y mosquitto
        - ip addr add 10.0.0.1/24 dev eth1
        - service mosquitto start

    # ATACANTE: Nó comprometido que gera o Flood
    attacker:
      kind: linux
      image: nicolaka/netshoot
      exec:
        - ip addr add 10.0.0.10/24 dev eth1

    # CLIENTE BENIGNO: Sensor legítimo para medir latência/disponibilidade
    client:
      kind: linux
      image: nicolaka/netshoot
      exec:
        - ip addr add 10.0.0.20/24 dev eth1

  links:
    # Todos ligados à Vítima (Gateway) na mesma interface lógica/física
    - endpoints: ["attacker:eth1", "victim:eth1"]
    - endpoints: ["client:eth1", "victim:eth1"]
